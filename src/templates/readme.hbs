# {{crate_name}}

Auto-generated configuration parser crate for {{config_filename}}.

## Usage

```rust
use {{crate_name}}::Root;
use rustemo::Parser;
use colap::ColaParser;
use colap::ModelBuilder;

// Parse a configuration file
fn parse_config() -> Root {
    let content = std::fs::read_to_string("path/to/config.md").expect("Failed to read config file");
    let parser = ColaParser::new();
    let result = parser.parse(&content).expect("Failed to parse configuration");
    let model = ModelBuilder::build_config_model(&result);
    Root::from_model(&model)
}

// Access configuration values
let config = parse_config();
{{#if example_code}}
{{example_code}}
{{/if}}
```

Generated by [colap](https://github.com/aivolution/colap) on {{date}}
